# MultiOmics-Reactome v3.0 -- pinned dependencies
# NumPy 2.x is the ABI anchor and MUST install first.
# The setup scripts enforce this order automatically.

numpy>=2.0.0,<3.0.0
scipy>=1.14.0
pandas>=2.2.0
scikit-learn>=1.5.0
statsmodels>=0.14.2

# Omics
anndata>=0.10.8
scanpy>=1.10.1
lxml>=5.2.0
networkx>=3.3

# Batch correction
#   harmonypy >= 0.0.11 compiles C++ and needs CMake + a system BLAS.
#   The setup scripts install 0.0.10 (pure Python) or a validated NumPy shim.
harmonypy<=0.0.10
#   pycombat replaces inmoose (which needs MSVC on Windows)
pycombat>=0.4.0

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0

# Web / cloud
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
boto3>=1.34.0
python-jose[cryptography]>=3.3.0
pyjwt[crypto]>=2.8.0
requests>=2.31.0

# Utility
joblib>=1.4.0
tqdm>=4.66.0

# Optional -- large speed-up, wheels exist for Python 3.9-3.12 only.
# Install manually if your Python version is supported:
#   pip install numba
# numba>=0.60.0

# Optional -- GPU/CPU NMF. Install from the PyTorch index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2.0

# Dev / test
pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0
ruff>=0.4.0
black>=24.0.0

# ── Optional integration back-ends ───────────────────────────────────────────
# The default back-end (NMF) needs nothing beyond the above. Install these to
# enable the corresponding method, then check with:
#     python -m backend.multiomics_reactome --list-methods
#
# MOFA+  (Argelaguet et al., Genome Biology 2020;21:111) - pure Python
mofapy2>=0.7.0
#
# SNF    (Wang et al., Nat Methods 2014;11:333)
#   Optional. A built-in NumPy implementation ships with the pipeline, so SNF
#   works without this package. snfpy <=0.2.2 calls scikit-learn's check_array
#   with force_all_finite, removed in scikit-learn 1.6; where that happens the
#   built-in implementation is used and a warning is logged.
snfpy>=0.2.2
#
# MCIA   (Meng et al., BMC Bioinformatics 2014;15:162)
#   No package required - implemented in NumPy. R/omicade4 not needed.
#
# DIABLO (Singh et al., Bioinformatics 2019;35:3055) - supervised
#   Requires R plus the Bioconductor package mixOmics:
#     1. Install R                 https://cran.r-project.org
#     2. In R: install.packages("BiocManager")
#              BiocManager::install("mixOmics")
#     3. pip install rpy2
# rpy2>=3.5.0
